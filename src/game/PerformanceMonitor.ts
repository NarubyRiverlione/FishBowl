import PerformanceService from '../services/monitoring/PerformanceService'

// Thin wrapper kept for compatibility with existing imports in the game layer.
export class PerformanceMonitor extends PerformanceService {}

export default PerformanceMonitor
