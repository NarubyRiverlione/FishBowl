/**
 * Represents the aquarium tank container.
 */
export interface ITank {
  /** Width of the tank in pixels */
  width: number

  /** Height of the tank in pixels */
  height: number

  /** Background color of the tank (hex string) */
  backgroundColor: number
}
