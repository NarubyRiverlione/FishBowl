# collision

## ✅ RESOLVED: Pause behavior issue

- ~~pause stops the ticker but fish keep swimming~~ **FIXED**: RenderingEngine now checks `isPaused` state before updating fish physics

# health

- health should regenerate (slowly), of course not above 100M

# message bar

Add a footer bar to display automatically messages like "a fish is very hunger", "a fish has pore health", "water is dirty"
